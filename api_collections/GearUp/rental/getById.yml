info:
  name: getRentalById
  type: http
  seq: 13

http:
  method: GET
  url: "{{localurl}}/api/rentals/{{rentalId}}"
  auth: bearer
  headers:
    - name: Authorization
      value: "Bearer {{customerToken}}"

runtime:
  variables:
    - name: customerToken
      value: ""
    - name: rentalId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
