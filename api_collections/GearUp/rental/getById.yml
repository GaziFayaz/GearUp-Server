info:
  name: getRentalById
  type: http
  seq: 13

http:
  method: GET
  url: "{{localurl}}/api/rentals/{{rental1Id}}"
  auth: bearer
  headers:
    - name: Authorization
      value: "Bearer {{customerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
