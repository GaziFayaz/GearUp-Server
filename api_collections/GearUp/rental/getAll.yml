info:
  name: getUserRentals
  type: http
  seq: 12

http:
  method: GET
  url: "{{localurl}}/api/rentals"
  auth: bearer
  headers:
    - name: Authorization
      value: "Bearer {{customerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
