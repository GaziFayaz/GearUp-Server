info:
  name: updateOrderStatus
  type: http
  seq: 14

http:
  method: PATCH
  url: "{{localurl}}/api/provider/orders/{{rental1Id}}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{providerToken}}"
  body:
    type: json
    data: |-
      { "status": "CONFIRMED" }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
