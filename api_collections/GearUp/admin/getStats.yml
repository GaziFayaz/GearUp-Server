info:
  name: getStats
  type: http
  seq: 19

http:
  method: GET
  url: "{{localurl}}/api/admin/stats"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
