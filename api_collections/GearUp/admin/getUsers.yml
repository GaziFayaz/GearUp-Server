info:
  name: getUsers
  type: http
  seq: 18

http:
  method: GET
  url: "{{localurl}}/api/admin/users"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
