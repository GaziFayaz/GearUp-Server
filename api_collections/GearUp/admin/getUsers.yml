info:
  name: getUsers
  type: http
  seq: 18

http:
  method: GET
  url: "{{localurl}}/api/admin/users"
  auth: bearer
  headers:
    - name: Authorization
      value: "Bearer {{adminToken}}"

runtime:
  variables:
    - name: adminToken
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
