info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: "{{localurl}}/api/health"
  auth: inherit

runtime:
  variables:
    - name: localurl
      value: http://localhost:4000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
