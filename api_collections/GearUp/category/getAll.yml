info:
  name: getAllCategories
  type: http
  seq: 10

http:
  method: GET
  url: "{{localurl}}/api/categories"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
