info:
  name: getUserPayments
  type: http
  seq: 16

http:
  method: GET
  url: "{{localurl}}/api/payments"
  auth: bearer
  headers:
    - name: Authorization
      value: "Bearer {{customerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
