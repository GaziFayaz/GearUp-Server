info:
  name: createPayment
  type: http
  seq: 15

http:
  method: POST
  url: "{{localurl}}/api/payments/create"
  headers:
    - name: Content-Type
      value: application/json
    - name: Authorization
      value: "Bearer {{customerToken}}"
  body:
    type: json
    data: |-
      { "rentalId": "{{rental1Id}}", "method": "STRIPE" }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
