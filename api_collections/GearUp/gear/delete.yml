info:
  name: deleteGear
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{localurl}}/api/provider/gear/{{gearId}}"
  auth: bearer
  headers:
    - name: Authorization
      value: "Bearer {{providerToken}}"

runtime:
  variables:
    - name: providerToken
      value: ""
    - name: gearId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
