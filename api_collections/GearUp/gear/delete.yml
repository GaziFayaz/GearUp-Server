info:
  name: deleteGear
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{localurl}}/api/provider/gear/{{gear1Id}}"
  auth: bearer
  headers:
    - name: Authorization
      value: "Bearer {{providerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
